# Required: Bootstrap node multiaddress
BOOTSTRAP_ADDR=/ip4/x.x.x.x/tcp/9100/p2p/peerID

# Optional: Port to listen on (default: 8001)
LISTEN_PORT=8001

# Optional: Public IP to advertise (if running behind NAT/proxy)
# PUBLIC_IP=1.2.3.4